.env
frontend/.env
frontend/node_modules
node_modules

# secrets — leaked before, never again
credentials.json
token.json
client_secret_*.json

# compiled binary, don't commit build output
api_server
api_server.exe

photo_*.jpg
firebase-debug.log
